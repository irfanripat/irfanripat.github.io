/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/irfanripat',
  assetPrefix: '/irfanripat/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
